<?php

return [

    // 'paths' => ['api/*', 'picker/*'],  // Include your custom path like 'picker/*'

    // 'allowed_methods' => ['*'],  // Allow all methods (GET, POST, etc.)

    // 'allowed_origins' => ['https://panel.portfolio.test'],  // Allow the specific origin making the request

    // 'allowed_origins_patterns' => [],

    // 'allowed_headers' => ['*'],  // Allow all headers

    // 'exposed_headers' => [],

    // 'max_age' => 0,

    // 'supports_credentials' => false,  // Set to true if using cookies, sessions, etc.
];
